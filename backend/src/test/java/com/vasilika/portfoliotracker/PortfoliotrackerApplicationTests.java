package com.vasilika.portfoliotracker;

import org.junit.jupiter.api.Disabled;
import org.springframework.boot.test.context.SpringBootTest;

@Disabled("Local build: requires DB + env. Enable in CI once test profile is set up.")
@SpringBootTest
class PortfoliotrackerApplicationTests {
	// contextLoads test removed or disabled
}
